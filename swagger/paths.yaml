/:
  options:
    description: Return description of API
    responses:
      200:
       $ref: responses.yaml#/option

/users:
  $ref: paths/users.yaml

/users/{userId}:
  $ref: paths/usersUserId.yaml

/users/{userId}/streams:
  $ref: paths/usersUserIdStreams.yaml

/users/{userId}/streams/{streamId}:
  $ref: paths/usersUserIdStreamsStreamId.yaml

/users/{userId}/streams/{streamId}/replay:
  $ref: paths/usersUserIdStreamsStreamIdReplay.yaml
