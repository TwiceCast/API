get:
  tags:
    - Channel
  description: Return one channel
  responses:
    200:
      $ref: ../responses.yaml#/channel
put:
  tags:
    - Channel
  description: Replace one channel
  responses:
    200:
      $ref: ../responses.yaml#/channel
patch:
  tags:
    - Channel
  description: Patch one channel
  responses:
    200:
      $ref: ../responses.yaml#/channel
delete:
  tags:
    - Channel
  description: delete a channel
  responses:
    200:
      description: done
parameters:
  - $ref: ../parameters.yaml#/channelId
  - $ref: ../parameters.yaml#/signature
