get:
  tags:
    - category
  description: Return one category
  responses:
    200:
      $ref: ../responses.yaml#/category
put:
  tags:
    - category
  description: Replace one category
  responses:
    200:
      $ref: ../responses.yaml#/category
patch:
  tags:
    - category
  description: Patch one category
  responses:
    200:
      $ref: ../responses.yaml#/category
delete:
  tags:
    - category
  description: Delete a category
  responses:
    200:
      description: Done
parameters:
  - $ref: ../parameters.yaml#/categoryId
