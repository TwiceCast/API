get:
  tags:
    - Organization
  description: Get users right on the stream
  responses:
    200:
      description: todo
parameters:
  - $ref: ../parameters.yaml#/organizationId
  - $ref: ../parameters.yaml#/signature
