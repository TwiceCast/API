get:
  description: Ruten one user
  responses:
    200:
      $ref: ../../responses.yaml#/user
parameters:
  - $ref: ../../parameters.yaml#/id
put:
  description: replace one user
  responses:
    200:
      $ref: ../../responses.yaml#/user
patch:
  description: patch one user
  responses:
    200:
      $ref: ../../responses.yaml#/user
delete:
  description: delete a user
  responses:
    200:
      description: done
