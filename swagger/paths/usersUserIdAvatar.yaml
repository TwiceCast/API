get:
  description: Return avatar
  responses:
    200:
      description: todo

put:
  description: replace avatar
  responses:
    200:
      description: todo
parameters:
  - $ref: ../parameters.yaml#/userId
