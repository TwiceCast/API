get:
  tags:
    - Auth
  description: Auth user based on signature
  responses:
    200:
      $ref: ../responses.yaml#/user
    401:
      description: unauthorized
      schema:
        $ref: ../definitions.yaml#/error
parameters:
  - $ref: ../parameters.yaml#/signature
