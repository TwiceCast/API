stream:
  $ref: definitions/stream.yaml

channel:
  $ref: definitions/channel.yaml

user:
  $ref: definitions/user.yaml

country:
  $ref: definitions/country.yaml

rank:
  $ref: definitions/rank.yaml

error:
  $ref: definitions/error.yaml

category:
  $ref: definitions/category.yaml
